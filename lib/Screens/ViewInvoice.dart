import 'package:flutter/material.dart';

class ViewInvoice extends StatelessWidget {
  const ViewInvoice({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Visor de cotización'),
      ),
    );
  }
}
